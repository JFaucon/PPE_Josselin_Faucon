{% extends 'base.html.twig' %}

{% block title %}Worktogether Data Solution{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">
            {{ message }}
        </div>
    {% endfor %}
    <div class="homepage text-center">
        <img src="{{ asset('img/brand.png') }}" alt="Worktogether Data Solution">
        <h1>Bienvenue chez Worktogether Data Solution</h1>
        <p class="mt-3">Nous sommes votre partenaire de confiance pour la location de baies de serveur.</p>
        <p>Notre mission est de vous fournir des solutions d'hébergement fiables et sécurisées pour répondre à vos besoins en matière d'infrastructure informatique.</p>
    </div>

    <h2 class="text-center my-4">Nos offres</h2>
    <div class="forfait">
        {% for forfait in forfaits %}
            <div class="card">
                <img class="card-img-top" src="{{ forfait.imgPath }}" alt="{{ forfait.name }}">
                <div class="card-body">
                    {% if forfait.discount != 0 %}
                    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-info">-{{ forfait.discount }}% </span>
                    {% endif %}
                    <h5 class="card-title">{{ forfait.name }}</h5>
                    <p class="card-text">{% if forfait.discount != 0 %}<span class="oldPrice">{{ (forfait.discount/100+1)*forfait.price }}€</span>{% endif %}{{ forfait.price }}€</p>
                    <p class="card-text">{{ forfait.nbSlot }} unité{{ forfait.nbSlot > 1 ? 's' : '' }} disponible{{ forfait.nbSlot > 1 ? 's' : '' }}</p>
                    <p class="card-text">Durée de {{ forfait.nbMonth }} mois</p>
                    <a href="{{ path('app_reservation', {'idForfait': forfait.id}) }}" class="btn btn-info">Obtenir</a>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}